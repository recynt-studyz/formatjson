import type { Metadata } from 'next'
import AdBanner from '@/components/AdBanner'
import RepairWrapper from '@/components/RepairWrapper'
import FAQ, { type FaqItem } from '@/components/FAQ'
import Footer from '@/components/Footer'

export const metadata: Metadata = {
  title: 'JSON Repair — Fix Broken JSON Online | formatjson.app',
  description: 'Auto-fix common JSON errors: trailing commas, single quotes, unquoted keys, and JavaScript comments. Free, instant, runs in your browser.',
  alternates: { canonical: 'https://formatjson.app/repair' },
  robots: { index: true, follow: true },
}

const faqItems: FaqItem[] = [
  {
    q: 'What JSON errors can this fix automatically?',
    a: 'The JSON repair tool fixes: trailing commas before } or ] (the most common error), JavaScript-style line comments (// ...) and block comments (/* ... */), single-quoted strings replaced with double quotes, and unquoted object keys (e.g. {key: "value"} → {"key": "value"}). After applying all fixes, it attempts JSON.parse and shows what was changed.',
  },
  {
    q: 'Will JSON repair change my data?',
    a: 'No. The repair tool only removes or corrects syntax issues — it never changes the actual values or structure of your data. Trailing commas, comments, and quote style are syntax artifacts; removing them does not alter what the JSON represents.',
  },
  {
    q: 'What causes trailing comma errors in JSON?',
    a: 'Trailing commas are allowed in JavaScript objects and arrays, but JSON (RFC 8259) explicitly forbids them. They appear when JSON is written by hand or generated by code that mimics JS syntax. For example: {"name": "Alice",} is valid JavaScript but invalid JSON. The repair tool removes these automatically.',
  },
  {
    q: 'Can I repair JSON with JavaScript comments?',
    a: 'Yes. JSON does not support comments (they were intentionally excluded from the spec), but developers often add // or /* */ comments when writing configuration files. The repair tool strips both line comments and block comments before attempting to parse.',
  },
  {
    q: 'Is my JSON sent to a server during repair?',
    a: 'No. All repair logic runs entirely in your browser using native JavaScript. Your JSON never leaves your device. You can verify this by opening DevTools → Network tab and watching for zero outbound requests while the tool runs.',
  },
]

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqItems.map((item) => ({
    '@type': 'Question',
    name: item.q,
    acceptedAnswer: { '@type': 'Answer', text: item.a },
  })),
}

const howToSchema = {
  '@context': 'https://schema.org',
  '@type': 'HowTo',
  name: 'How to Repair Broken JSON Online',
  step: [
    { '@type': 'HowToStep', name: 'Paste your broken JSON', text: 'Paste the broken or invalid JSON into the left input panel.' },
    { '@type': 'HowToStep', name: 'Repair runs automatically', text: 'The tool automatically removes trailing commas, comments, and single quotes, and quotes unquoted keys.' },
    { '@type': 'HowToStep', name: 'Copy or download the result', text: 'If repair succeeds, copy the fixed JSON or download it as a .json file.' },
  ],
}

const webAppSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebApplication',
  name: 'JSON Repair Tool',
  url: 'https://formatjson.app/repair',
  description: 'Free online JSON repair tool. Auto-fix trailing commas, comments, single quotes, and unquoted keys. Runs entirely in your browser.',
  applicationCategory: 'DeveloperApplication',
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
}

export default function RepairPage() {
  return (
    <>
      <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faqSchema).replace(/</g, '\\u003c') }} />
      <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(howToSchema).replace(/</g, '\\u003c') }} />
      <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(webAppSchema).replace(/</g, '\\u003c') }} />

      <h1 className="sr-only">JSON Repair — Fix Broken JSON Online</h1>
      <RepairWrapper />

      <section className="bg-white dark:bg-[#0f172a]">
        <div className="max-w-3xl mx-auto px-4 sm:px-6 pb-10">
          <p className="text-sm text-gray-600 dark:text-gray-400 mb-8 leading-relaxed">
            This <strong>JSON repair tool</strong> automatically fixes the most common JSON syntax
            errors — trailing commas, JavaScript-style comments, single-quoted strings, and unquoted
            keys. All repair logic runs entirely in your browser using native JavaScript.{' '}
            <strong>Your JSON never leaves your device.</strong> Paste broken JSON on the left and
            get corrected, formatted output on the right instantly.
          </p>
          <FAQ questions={faqItems} />
        </div>
        <div className="max-w-3xl mx-auto px-4 sm:px-6 pb-6">
          <AdBanner slot="1234509876" />
        </div>
      </section>

      <Footer />
    </>
  )
}
